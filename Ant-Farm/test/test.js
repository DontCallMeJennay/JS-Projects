var assert = require('assert');
var should = require('chai').should();
var expect = require('chai').expect();

describe('trackDistance', function() {

    it('should keep track of distance from nest', function() {
        
    });
});

describe('goHome', function() {
	
    it('should increment or decrement as the ant moves toward home', function() {
        
    });
});